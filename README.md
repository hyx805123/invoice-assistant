# 发票小助手 v3.1

一款轻量级发票批量处理工具，自动提取发票**日期、价税合计金额**，结合**开具单位 + 发票内容**智能识别发票类型，按 `年月-类型-金额` 格式统一重命名并归类整理。

> **仓库地址**：https://github.com/hyx805123/invoice-assistant
>
> **下载 EXE**：前往 [Releases](https://github.com/hyx805123/invoice-assistant/releases/tag/v3.1) 页面下载
> `InvoiceAssistant-v3.1.exe`（80.9 MB），免安装、无需 Python 环境，双击即可运行。
>
> 注：GitHub 附件名不支持中文，故 Release 附件使用英文名 `InvoiceAssistant-v3.1.exe`，
> 与本地打包产物 `发票小助手-v3.1.exe` 为同一文件。

## 版本说明

- **v3.1**（当前）：新增软件图标、窗口角标，优化打包体积，发布为新版本 `dist/发票小助手-v3.1.exe`，旧版 `InvoiceSorter.exe` 保留。
- **v3.0**：首版完整功能（OCR、分类、重命名、统计报告）。

## 功能特点

- **多格式支持**：PDF、JPG、JPEG、PNG、BMP、TIFF、WEBP
- **智能 OCR**：图片发票与扫描件 PDF 自动识别（内置轻量中文 OCR 引擎，无需联网、无需外部程序）
- **智能分类**：综合「货物/服务名称」与「销售方（开具单位）」两级关键词自主判断，覆盖交通、餐饮、住宿、办公、通讯、物流、服务、医疗、水电、差旅、教育培训、图书、广告宣传等类型
- **自动提取**：精准提取「价税合计（小写）」金额与开票日期
- **统一命名**：`2026年8月-交通类-820.00元.pdf`
- **多种归类方式**：
  - 按月份+类型分类（推荐）
  - 按月份归类
  - 按类型分类
  - 覆盖源文件（原地重命名）
- **批量处理**：支持单个/多个文件或整个文件夹
- **处理报告**：自动生成明细 + 分类金额统计
- **一键自检**：命令行运行 `发票小助手-v3.1.exe --selftest` 可生成诊断结果

## 使用说明（EXE 版，推荐）

1. 从 [Releases](https://github.com/hyx805123/invoice-assistant/releases/tag/v3.1) 下载 `发票小助手-v3.1.exe`
2. 双击运行（无需安装 Python）
2. 点击「选择文件」或「选择文件夹」
3. （可选）点击「选择输出文件夹」，不选则默认在源文件夹旁创建 `xx_处理结果`
4. 选择归类方式（默认「按月份+类型分类」）
5. 点击「开始处理」
6. 完成后查看结果与 `处理报告.txt`

## 分类规则

| 分类 | 匹配依据（内容 / 开具单位示例） |
|------|----------------|
| 交通类 | 滴滴、高铁、火车、机票、加油、停车、ETC、铁路、中石化 |
| 餐饮类 | 餐饮、餐费、外卖、美团、饿了么、海底捞、星巴克 |
| 住宿类 | 住宿、酒店、宾馆、客房、民宿 |
| 办公类 | 办公、打印、文具、电脑、软件、京东、联想 |
| 通讯类 | 话费、流量、宽带、电信、移动、联通 |
| 物流类 | 快递、物流、运费、顺丰、圆通、中通 |
| 服务类 | 服务费、咨询、培训、维修、设计、广告 |
| 医疗类 | 医疗、药品、诊疗、体检、医院、药店 |
| 水电类 | 水费、电费、燃气、物业、房租 |
| 差旅类 | 差旅、出差、机票、酒店 |
| 教育培训类 | 培训、教育、课程、学费、网课 |
| 图书类 | 图书、书籍、教材、出版 |
| 广告宣传类 | 广告、宣传、推广、印刷 |

> 无法识别的发票归入「其他」类。

## 开发者指南

### 环境要求

- Python 3.8+
- 依赖包见 `requirements.txt`

### 源码运行

```bash
pip install -r requirements.txt
python invoice_processor.py
```

### 打包为 EXE

```bash
# 方式一：使用打包脚本（推荐）
双击 build.bat

# 方式二：使用自定义 spec 手动打包
pyinstaller --noconfirm 发票小助手-v3.1.spec
```

打包后的 EXE 位于 `dist/` 目录。脚本会自动保留旧版 EXE，不覆盖历史版本。

> **体积说明**
>
> - 当前 `发票小助手-v3.1.exe` 约 **84.8 MB**（约 80.9 MiB），已通过两层优化：①排除未使用的 OpenCV FFmpeg、PIL AVIF 插件等冗余二进制；②用 [UPX](https://upx.github.io/) 压缩打包内嵌的 DLL/pyd。
> - 较 v3.0（123,418,777 字节 / 约 117.7 MiB）整体下降约 **31%**。
> - 由于内置 OCR 引擎（含 ONNX 模型），EXE 体积很难再大幅缩减；项目目录已内置 `upx.exe`，`build.bat` 会自动检测并调用它完成 UPX 压缩。

## 文件说明

| 文件 | 说明 |
|------|------|
| `invoice_processor.py` | 主程序源码 |
| `requirements.txt` | Python 依赖配置 |
| `build.bat` | EXE 打包脚本（自动保留历史版本） |
| `发票小助手-v3.1.spec` | PyInstaller 自定义打包配置（含体积优化与图标） |
| `logo.ico` / `logo.png` | 软件图标 |
| `README.md` | 项目文档 |

## 注意事项

- 图片清晰度直接影响 OCR 识别率，请尽量使用原始清晰发票
- 覆盖源文件模式会直接重命名原始文件，操作不可撤销，请谨慎使用
- 金额提取以「价税合计（小写）」为主要匹配目标

## 版权信息

Copyright (c) 2026 yingxiang.he@zkh.com. All rights reserved.
